# Backend Rules

ဤဖိုဒါသည် backend coding rules နှင့် patterns များအတွက်ဖြစ်သည်။

Add backend-specific coding rules and patterns here as separate `.md` files
(e.g., `models.md`, `routes.md`, `dates.md`). Every rule added follows the
project's strict workflow: plan first, save to `backend/plans/`, wait for approval.